[pytest]
testpaths = tests
addopts = -q --no-header
filterwarnings =
    # Los xlsx del kit oficial no traen hoja de estilos. Es inocuo y ensucia la salida.
    ignore:Workbook contains no default style:UserWarning
